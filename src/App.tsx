import {Button} from "@mui/material";

function App() {

  return (
    <main className={"w-full h-full flex justify-center items-center"}>
     <span className={""}>
      <Button className={""}>
        Test
      </Button>
     </span>
    </main>
  )
}

export default App
